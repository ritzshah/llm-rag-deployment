kind: Service
apiVersion: v1
metadata:
  name: postgresql
  namespace: ic-shared-llm
  labels:
    app: ic-shared-llm
spec:
  selector:
    app: ic-shared-llm
  ports:
    - name: postgresql
      protocol: TCP
      port: 5432
      targetPort: 5432
