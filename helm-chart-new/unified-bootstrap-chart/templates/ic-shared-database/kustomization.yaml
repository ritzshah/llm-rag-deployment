---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

commonLabels:
  component: ic-shared-database

resources:
  - db-init-job.yaml
  - deployment.yaml
  - populate-images.yaml
  - presync.yaml
  - pvc.yaml
  - rbac-presync-monitoring.yaml
  - secret-minio.yaml
  - secret.yaml
  - service.yaml
  - sql-script-configmap.yaml

transformers:
  - namespace-transformer.yaml
