1. Get the application URL by running these commands:
{{- if .Values.route.enabled }}
  You can access your application via the OpenShift route:
  
  Route URL: https://{{ include "gradio-llm-rag.fullname" . }}-route-{{ .Values.namespace.name }}.apps.<your-cluster-domain>
  
  To get the exact route URL, run:
  kubectl get route {{ include "gradio-llm-rag.fullname" . }}-route -n {{ .Values.namespace.name }}

{{- else }}
  Port-forward to access the application locally:
  kubectl port-forward --namespace {{ .Values.namespace.name }} svc/{{ include "gradio-llm-rag.fullname" . }}-service {{ .Values.service.port }}:{{ .Values.service.port }}

  Then visit: http://127.0.0.1:{{ .Values.service.port }}
{{- end }}

2. Check the status of your deployment:
  kubectl get pods -n {{ .Values.namespace.name }} -l app.kubernetes.io/instance={{ .Release.Name }}

3. View application logs:
  kubectl logs -n {{ .Values.namespace.name }} deployment/{{ include "gradio-llm-rag.fullname" . }}

4. Application Configuration:
  - App Title: {{ .Values.app.title }}
  - Model: {{ .Values.llm.modelName }}
  - Debug UI: {{ .Values.app.enableDebugUI }}
  - Database Collection: {{ .Values.database.collectionName }}

{{- if not .Values.route.enabled }}
WARNING: OpenShift Route is disabled. The application will only be accessible within the cluster or via port-forwarding.
{{- end }}

For more information about this chart, visit the README.md file in the chart directory.

Happy RAG chatting! 🤖📚 